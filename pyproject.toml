[project]
name = "agent-merge-test"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv>=1.0",
  "livekit-agents[openai,silero,deepgram]~=1.0",
]

[dependency-groups]
dev = [
  "ruff>=0.15.0",
]


[tool.ruff.lint]
select = ["E", "F", "I"]
